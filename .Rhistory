library(readr)
# Read the CSV file
data <- read.csv("friday_data.csv")
library(readr)
# Read the CSV file
data <- read.csv("friday_data.csv")
setwd("~/Library/CloudStorage/GoogleDrive-silas.raye@gmail.com/My Drive/Zipline")
library(readr)
# Read the CSV file
data <- read.csv("friday_data.csv")
# Convert columns to factors
data$IsRushHr <- factor(data$IsRushHr)
data$Is352 <- factor(data$Is352)
# Filter out wait times less than 0 or greater than 120
data <- data[data$WaitTime >= 0 & data$WaitTime <= 120, ]
# Perform linear regression
model <- lm(WaitTime ~ NumOpenTickets + IsRushHr + Is352 + NumSilces + NumMed + NumLrg + NumPan + NumZone + NumMFP, data=data)
# Print the summary of the regression model
summary(model)
library(readr)
# Read the CSV file
data <- read.csv("saturday_data.csv")
# Convert columns to factors
data$IsRushHr <- factor(data$IsRushHr)
data$Is352 <- factor(data$Is352)
# Filter out wait times less than 0 or greater than 120
data <- data[data$WaitTime >= 0 & data$WaitTime <= 120, ]
# Perform linear regression
model <- lm(WaitTime ~ NumOpenTickets + IsRushHr + Is352 + NumSilces + NumMed + NumLrg + NumPan + NumZone + NumMFP, data=data)
# Print the summary of the regression model
summary(model)
# Read the CSV file
data <- read.csv("weekdays_data.csv")
library(readr)
# Read the CSV file
data <- read.csv("weekday_data.csv")
# Convert columns to factors
data$IsRushHr <- factor(data$IsRushHr)
data$Is352 <- factor(data$Is352)
# Filter out wait times less than 0 or greater than 120
data <- data[data$WaitTime >= 0 & data$WaitTime <= 120, ]
# Perform linear regression
model <- lm(WaitTime ~ NumOpenTickets + IsRushHr + Is352 + NumSilces + NumMed + NumLrg + NumPan + NumZone + NumMFP, data=data)
# Print the summary of the regression model
summary(model)
